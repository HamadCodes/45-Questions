function make_shirt(size = "large", message = "I love TypeScript") {
    console.log(`Your shirt size is ${size} and this is the message written on it "${message}"`);
}

make_shirt();
make_shirt("medium");
make_shirt("small", "JavaScript is awesome!")
